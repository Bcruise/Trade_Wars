import '../css/Leaderboard.css';

function Leaderboard({setWhichComponent}) {

    // don't show leaderboard link but show return link
    setWhichComponent('Leaderboard');

    return (
      <div className="leaderboard">
        
      </div>
    );
  }
  
  export default Leaderboard;